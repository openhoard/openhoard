{
  "name": "@openhoard/core-identity",
  "version": "0.0.0",
  "private": true,
  "description": "Identity: users, groups, memberships and who a signed-in person is to authorize().",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "clean": "rimraf dist coverage"
  },
  "dependencies": {
    "@openhoard/core-db": "workspace:^",
    "@openhoard/core-policy": "workspace:^",
    "drizzle-orm": "^0.45.3"
  },
  "devDependencies": {
    "@openhoard/config": "workspace:*",
    "@openhoard/core-audit": "workspace:*"
  },
  "license": "Apache-2.0"
}
