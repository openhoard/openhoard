{
  "name": "@openhoard/core-policy",
  "version": "0.0.0",
  "private": true,
  "description": "Policy: visibility, exposure and authorization decisions.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "clean": "rimraf dist coverage"
  },
  "devDependencies": {
    "@openhoard/config": "workspace:*"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@cedar-policy/cedar-wasm": "4.13.0"
  }
}
